/**
 * @file lv_drivers.h
 * Minimal driver aggregate header for the gimbal LVGL port.
 *
 * The project uses a custom ST7789 flush callback and ADC key input port, so
 * LVGL's built-in display/input drivers are not enabled or compiled.
 */

#ifndef LV_DRIVERS_H
#define LV_DRIVERS_H

#endif
